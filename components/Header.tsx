import React from 'react';
import LocaleSwitcher from '@/components/LocaleSwitcher';

const Header = () => {
    return (
        <div>
            <LocaleSwitcher />
        </div>
    );
};

export default Header;